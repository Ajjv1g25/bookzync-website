import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Contact" };

export default function ContactPage() {
  return (
    <PlaceholderPage
      title="Let's talk."
      subtitle="Schedule a tour, ask a question, or start onboarding. We'll get back to you within one business day."
    />
  );
}
